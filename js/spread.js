const max = Math.max(12, 85, 99, 78);
// console.log(max);
const numbers = [12, 85, 99, 78];
console.log(...numbers); // to get the only value of array use three dots(...)
const largest = Math.max(...numbers);
console.log(largest);
